[project]
name = "server"
version = "0.1.0"
description = "Static file server for the frontend application"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "litestar>=2.24.0",
    "uvicorn>=0.51.0",
]
